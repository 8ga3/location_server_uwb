# AGENTS.md

## 文章の書き方

**ドキュメント (`doc/*.md`、README など) とソースコード中のコメントは、すべて通常の日本語で書く。**

- 会話側で圧縮した口調を使っている場合でも、ファイルに書き込む文章には適用しない
- 敬語である必要はないが、助詞を省略した圧縮表現や体言止めの多用は避け、後から読んで意味が一意に
  取れる文にする
- コミットメッセージと PR 本文も同様に通常の日本語または英語で書く
- 既存ファイルの文体に合わせる。英語で書かれているコメントを日本語へ書き換えることはしない

## 設計の基準

開発は [`doc/server-design.md`](doc/server-design.md) に準ずる。同文書を本リポジトリの設計上の正本として扱い、
実装と文書が食い違う変更を行わない。

- 設計変更が必要な場合は、実装だけを先行させず `doc/server-design.md` も同じ変更で更新する
- 未決事項を独断で確定しない。実装に判断が必要な場合は確認を取るか、設計文書へ決定理由を追記する
- 実装ロードマップのフェーズと依存関係を尊重する。後続フェーズの都合で前段の責務を崩さない
- デバッグ・精度評価用の単一プロセスサーバーであり、運用系の高可用性を初版の要件に持ち込まない

## アーキテクチャ

サーバーは 1 プロセスで動かすが、次の責務を内部で分離する。

1. 構成配信: アンカー ID、設置座標、バイアス補正値を管理し、HTTP API でタグへ配信する
2. テレメトリ収集: UDP パケットを検証・展開し、SQLite へ一括保存する
3. 参照・可視化: 蓄積データを参照 API で返し、ライブデータを WebSocket で配信する

次の境界を守る。

- 測位計算はタグ側で完結させる。サーバーへ測位アルゴリズムを持ち込まない
- サーバー障害や通信断がタグの測距・測位処理を停止させない構成を維持する
- 保存系とライブ配信系を分離し、一方の遅延や障害を他方へ波及させない
- ライブ配信は DB コミットを待たず、受信直後のパース結果を使う
- 初版は FastAPI、uvicorn、`asyncio.DatagramProtocol`、SQLite、WebSocket、自前の静的ページで構成する
- 初版ではコンテナ、外部メッセージブローカー、mDNS、認証基盤を追加しない

## データと通信の不変条件

- 座標は API の JSON でのみメートル表記とし、通信と DB 内部では整数ミリメートルで保持する
- アンカー ID は UWB の responder address そのものとし、別の通し番号を設けない
- タグの 1 回の起動を `(tag_id, boot_id)` で識別する 1 セッションとして扱う
- `seq` の欠番による UDP パケット欠測と、`status != 0` の測距失敗を区別して記録・表示する
- UDP パケットは `doc/server-design.md` のバイナリ形式に厳密に従い、magic、version、長さ、各値の範囲を
  DB 書き込み前に検証する
- UDP には ACK、再送、順序保証、ハンドシェイクを追加しない。遅延した過去データより最新データを優先する
- 遅い WebSocket クライアントのキューとライブ用リングバッファには上限を設け、溢れた場合は古いデータから
  捨てる。破棄数は観測可能にする
- SQLite は WAL モードかつ単一ライターとし、受信データは設計文書に従って一括トランザクションで保存する
- 構成変更ではリビジョンを単調増加させ、セッションが使用した構成を後から再現できるようにする
- 参照 API の大量データは `max_points` に従って間引き、可視化向けの列指向 JSON で返す

## エラー処理と可観測性

- 不正な UDP パケットで受信ループを停止させない。破棄理由ごとのカウンタを記録する
- DB 書き込み失敗、キュー溢れ、WebSocket のデータ破棄などを握り潰さず、ログまたはメトリクスで確認可能にする
- タグから 3 秒間パケットが届かない場合はセッション終了としてライブ購読者へ通知する
- 入力値を暗黙に補正しない。測位の発散値を含め、プロトコル上有効な値はデバッグ情報としてそのまま保存する
- 管理 API の共有トークンを有効にした場合、書き込み系 API にだけ一貫して適用する

## 実装とテスト

- Python 3.14 を基準とし、型注釈を付けて責務境界を明確にする
- UDP のデコード、入力検証、DB 操作、API スキーマ、WebSocket 配信、表示用データ変換を分離してテスト可能にする
- 外部依存を追加する前に、標準ライブラリまたは既存依存で実現できないか確認する
- 変更箇所に対応する最小範囲のテスト、lint、型チェックを実行する。プロジェクトの実行コマンドが整備された後は
  README または設定ファイルに記載されたコマンドを正とする
- UDP デコーダーでは正常系に加え、短いパケット、不正な magic/version、範囲外の count/anchor_n、
  レコード長不一致をテストする
- DB テストではセッションの自動生成、重複受信、トランザクション失敗、構成リビジョンの再現性を確認する
- API と WebSocket では設計文書に記載された応答形、ETag、snapshot/append、購読切り替え、
  バックプレッシャー時の破棄を確認する
- 可視化はライブと再生で描画ロジックを共有し、外部ネットワークへ接続せず動作する状態を保つ
- `tools/dump_udp.py` を UDP 送信側との結合確認に使える状態で維持する

## Markdown

`doc/*.md`、README、および本ファイルは markdownlint を通す。

- 見出しの代わりに太字 (`**...**`) だけの行を置かない (MD036)
- 1 行は 120 文字までとする。コードブロックとテーブルは対象外とする
